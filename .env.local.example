# Optional: OpenAlex API key for premium access.
# Without it the public polite pool is used (no key required).
# See: https://docs.openalex.org/how-to-use-the-api/rate-limits-and-authentication
OPENALEX_API_KEY=
